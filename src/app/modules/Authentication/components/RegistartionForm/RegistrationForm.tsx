import React from 'react';
import Input from 'app/components/controls/Input/Input';
import Types from 'app/components/controls/Input/types';
import InputEmail from 'app/modules/Authentication/components/controls/InputEmail';
import InputPassword from 'app/modules/Authentication/components/controls/InputPassword';

const RegistrationForm = () => {
    return (
        <form>
            {/*<InputEmail value={form.email} onChange={handleInputChange} />*/}
            {/*<InputPassword value={form.password} onChange={handleInputChange} />*/}
        </form>
    );
};

export default RegistrationForm;
