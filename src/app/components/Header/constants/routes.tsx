import ROUTES from 'app/constants/routes';

const items = [
    {
        route: '/',
        label: 'home',
    },
    {
        route: ROUTES.LOGIN,
        label: 'login',
    },
];

export default items;
